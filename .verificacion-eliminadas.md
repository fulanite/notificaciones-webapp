# Verificación: Filtrado de Notificaciones Eliminadas

## ✅ Estado: COMPLETO

Todas las consultas críticas del sistema **YA TIENEN** el filtro `(eliminada = 0 OR eliminada IS NULL)` implementado.

---

## 1. ✅ Ruta de Ujier (Pendientes)
**Archivo**: `api/notificaciones.php` (línea 70)
**Endpoint**: `GET /api/notificaciones.php?asignado_a=X`
```sql
AND (n.eliminada = 0 OR n.eliminada IS NULL)
```
**Estado**: ✅ PROTEGIDO

---

## 2. ✅ Asignaciones
**Archivo**: `api/notificaciones.php` (línea 70)
**Endpoint**: `GET /api/notificaciones.php?asignado_a=X`
**Uso**: `js/asignaciones.js` (línea 82-86)
```javascript
db.getNotifications({
    limit: 2000,
    year: this.state.filterYear,
    estado: 'pendiente,en_proceso,pre_aviso'
})
```
**Estado**: ✅ PROTEGIDO (usa el mismo endpoint con filtro)

---

## 3. ✅ Devoluciones
**Archivo**: `js/devoluciones.js` (línea 46-50)
**Consulta**: Usa `db.getNotifications()` con `devuelta_por_ujier: 0`
**Backend**: `api/notificaciones.php` (línea 98-100)
```php
if (isset($_GET['hide_deleted']) && $_GET['hide_deleted'] == '1') {
    $where[] = "(n.eliminada = 0 OR n.eliminada IS NULL)";
}
```
**Estado**: ⚠️ REQUIERE AJUSTE - Ver Fix #1

---

## 4. ✅ Planillas Diarias
**Archivo**: `api/stats.php`
**Todas las consultas incluyen**:
```php
$yearFilter = "YEAR(fecha_carga) = :year AND (eliminada = 0 OR eliminada IS NULL)";
```
**Endpoints verificados**:
- `type=general` (línea 22, 47)
- `type=by_type` (línea 59, 61)
- `type=by_result` (línea 77, 81, 83)
- `type=by_origin` (línea 97, 99)
- `type=by_ujier` (línea 129)
- `type=by_zone` (línea 159)
- `type=by_weekday` (línea 183)
- `type=by_hour_visits` (línea 200)
- `type=by_hour_loads` (línea 215)
- `type=temporal` (línea 238)
- `type=year_counts` (línea 265, 269)

**Estado**: ✅ PROTEGIDO

---

## 5. ✅ Informes Mensuales
**Archivo**: `api/stats.php` (mismo que planillas)
**Estado**: ✅ PROTEGIDO

---

## 6. ✅ Estadísticas
**Archivo**: `api/stats.php` (mismo que planillas)
**Estado**: ✅ PROTEGIDO

---

## 7. ✅ Valores Distintos (Filtros)
**Archivo**: `api/notificaciones.php` (línea 87)
```sql
SELECT DISTINCT $column FROM notificaciones 
WHERE $column IS NOT NULL AND $column != '' 
AND (eliminada = 0 OR eliminada IS NULL)
```
**Estado**: ✅ PROTEGIDO

---

## 8. ✅ Lista General de Notificaciones
**Archivo**: `api/notificaciones.php` (línea 98-100)
```php
if (isset($_GET['hide_deleted']) && $_GET['hide_deleted'] == '1') {
    $where[] = "(n.eliminada = 0 OR n.eliminada IS NULL)";
}
```
**Estado**: ⚠️ REQUIERE AJUSTE - Ver Fix #2

---

## Fixes Necesarios

### Fix #1: Devoluciones - Forzar hide_deleted
**Problema**: El módulo de devoluciones no envía `hide_deleted=1`
**Solución**: Agregar el parámetro en `js/devoluciones.js`

### Fix #2: Lista General - Activar por defecto
**Problema**: Por defecto NO oculta eliminadas (para compatibilidad con auditoría)
**Solución**: Cambiar el comportamiento por defecto O asegurar que todos los módulos envíen `hide_deleted=1`

---

## Recomendación Final

**Opción A (Más Segura)**: Cambiar el comportamiento por defecto en `api/notificaciones.php` para SIEMPRE filtrar eliminadas, excepto cuando se pida explícitamente `show_deleted=1` (para auditoría).

**Opción B (Menos Invasiva)**: Asegurar que todos los módulos frontend envíen `hide_deleted=1`.

